# Bootstrap Agent — Recommended Skills Index
# Skills that this agent should load and use
# Generated: 2026-03-02

agent: bootstrap
purpose: "Initialize or refresh project context, validate governance compliance"

core_skills: {}

cross_skills:
  analysis:
    - id: SKILL-SUMMARIZATION-001
      name: summarization
      when: "Compacting project memory after first pass"
      required: false

project_skills:
  governance:
    - id: SKILL-FRAMEWORK-SYNC-001
      name: framework-sync
      when: "Validating GAAI compliance, checking project setup"
      required: true
  
  knowledge:
    - id: SKILL-DOMAIN-KNOWLEDGE-001
      name: domain-knowledge-research
      when: "Understanding product context, market, competitors"
      required: true
    - id: SKILL-ANALYTICS-QUERY-001
      name: analytics-query
      when: "Understanding current product metrics and baselines"
      required: false

total_available: 4
required_skills: 2
