# Discovery Agent — Recommended Skills Index
# Skills that this agent should load and use
# Generated: 2026-03-02

agent: discovery
purpose: "Create artefacts (Epics, Stories, decisions) based on user intent and market signals"

core_skills:
  validation:
    - id: SKILL-VALIDATE-ARTEFACTS-001
      name: validate-artefacts
      when: "Before handing off artefacts to Delivery"
      required: true

cross_skills:
  translation:
    - id: SKILL-IDIOMATIQUE-TRANSLATE-001
      name: idiomatique-translate
      when: "Writing international copy, messaging in multiple languages"
      required: false
  
  analysis:
    - id: SKILL-I18N-EXTRACT-001
      name: i18n-extract
      when: "Planning i18n for features"
      required: false
    - id: SKILL-SUMMARIZATION-001
      name: summarization
      when: "Condensing memory, market research, or decision logs"
      required: false

project_skills:
  content:
    - id: SKILL-GOOGLE-TRENDS-001
      name: google-trends
      when: "Understanding market signals, competitor landscape"
      required: false
    - id: SKILL-CONTENT-PLAN-001
      name: content-plan
      when: "Planning product launch content, messaging"
      required: false
  
  analysis:
    - id: SKILL-DOMAIN-KNOWLEDGE-001
      name: domain-knowledge-research
      when: "Deep-diving into market, expert landscape, customer needs"
      required: true
    - id: SKILL-ANALYTICS-QUERY-001
      name: analytics-query
      when: "Understanding user behavior, validation data"
      required: false
    - id: SKILL-COST-ANALYSIS-001
      name: cost-analysis
      when: "Evaluating architectural trade-offs or scaling decisions"
      required: false

total_available: 10
required_skills: 2
