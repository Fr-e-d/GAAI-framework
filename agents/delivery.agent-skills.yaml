# Delivery Agent — Recommended Skills Index
# Skills that this agent should load and use
# Generated: 2026-03-02

agent: delivery
purpose: "Implement validated Stories, execute plans, ensure quality gates pass"

core_skills:
  execution:
    - id: SKILL-FRONTEND-DESIGN-001
      name: frontend-design
      when: "Building UI components, pages, or interfaces"
      required: false
    - id: SKILL-REMEDIATE-FAILURES-001
      name: remediate-failures
      when: "QA review returns FAIL, fixing acceptance criteria gaps"
      required: true

cross_skills:
  translation:
    - id: SKILL-IDIOMATIQUE-TRANSLATE-001
      name: idiomatique-translate
      when: "Translating UI copy during implementation, i18n work"
      required: false
  
  validation:
    - id: SKILL-I18N-EXTRACT-001
      name: i18n-extract
      when: "Validating hardcoded strings during refactor"
      required: false
    - id: SKILL-I18N-VALIDATE-001
      name: i18n-validate
      when: "Quality gate: verifying translation completeness"
      required: false
    - id: SKILL-I18N-GLOSSARY-SYNC-001
      name: i18n-glossary-sync
      when: "Ensuring terminology consistency in implementation"
      required: false

project_skills:
  governance:
    - id: SKILL-FRAMEWORK-SYNC-001
      name: framework-sync
      when: "Checking code compliance with GAAI patterns and conventions"
      required: true
  
  content:
    - id: SKILL-REPURPOSE-001
      name: repurpose
      when: "Creating supporting content for feature launch"
      required: false
    - id: SKILL-SOCIAL-ADAPT-001
      name: social-adapt
      when: "Adapting feature announcements for different platforms"
      required: false

total_available: 11
required_skills: 2
